function gerarPDF() {
    // Instanciar o jsPDF
    var doc = new jsPDF();
    // Conteúdo HTML, que deve estar no PDF 
    doc.html('<p class="titulo">Relatório</p>');
    // Gerar PDF 
    doc.save('relatorio.pdf');
}