const path = require('path');

module.exports = {
  mode: 'development', // ou 'production' se estiver pronto para produção
  entry: './index.js', // Alterado para o nome do seu arquivo JS principal
  output: {
    filename: 'bundle.js',
    path: path.resolve(__dirname, 'dist')
  }
};